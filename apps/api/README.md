
# ⚙️ Quantum Threat Shield — API

**Quantum Threat Shield API** — это центральный сервис платформы QTS, отвечающий за обработку, хранение и предоставление данных об угрозах и уязвимостях, а также за взаимодействие между инфраструктурными, аналитическими и пользовательскими компонентами системы.

API является ядром платформы и реализует доменную логику безопасности.

---

## 🎯 Назначение сервиса

API выполняет следующие функции:

* приём и нормализация данных об угрозах и уязвимостях
* управление доменными сущностями безопасности
* хранение и версионирование данных
* предоставление HTTP API для внешних и внутренних клиентов
* обеспечение целостности и консистентности данных
* интеграция с аналитическими и AI-модулями

---

## 🧠 Архитектура сервиса

Сервис построен по принципам:

* **Layered Architecture**
* **Domain-driven design (DDD, упрощённый)**
* **Явное разделение инфраструктуры и бизнес-логики**
* **Минимальная связность между модулями**

---

## 🧱 Архитектурные слои

### 1️⃣ Transport Layer

Отвечает за внешний интерфейс сервиса.

* HTTP endpoints
* маршрутизация
* валидация входных данных
* маппинг DTO → доменные структуры

**Технологии:**

* Axum
* Tower
* Serde

---

### 2️⃣ Application Layer

Слой прикладной логики:

* orchestration use-case’ов
* транзакционное управление
* бизнес-правила
* контроль доступа

---

### 3️⃣ Domain Layer

Ключевая доменная модель:

* Threat
* Vulnerability
* Source
* Severity
* Correlation

Содержит:

* инварианты
* бизнес-ограничения
* доменные события

---

### 4️⃣ Infrastructure Layer

Техническая реализация:

* PostgreSQL
* SQLx
* Redis
* внешние источники данных
* файловая система

---

## 🧩 Основные доменные модули

### 🔐 Threat Management

* хранение и классификация угроз
* жизненный цикл угроз
* связь с уязвимостями
* источники и доверие к данным

---

### 🧨 Vulnerability Management

* уязвимости (CVE, CVSS)
* версии и обновления
* привязка к продуктам и системам
* приоритеты устранения

---

### 🔗 Correlation Engine

* связи «угроза → уязвимость»
* агрегация сигналов
* дедупликация
* контекстная корреляция

---

### 📡 Data Ingestion

* импорт данных из внешних источников
* парсинг и нормализация
* валидация данных
* управление источниками

---

## 🗄️ Хранение данных

### PostgreSQL

Используется как основное хранилище:

* нормализованная схема
* поддержка enum / jsonb
* миграции через `sqlx`

Миграции хранятся в:

```text
apps/api/migrations
```

---

### Redis

Используется для:

* кеширования запросов
* временных данных
* очередей и сигналов

---

## 📑 API Контракты

* REST / JSON
* OpenAPI спецификация
* версионирование API (`/v1`, `/v2`)

Пример:

```http
GET /v1/threat
```

---

## 🧰 Технологический стек

| Категория     | Технологии             |
| ------------- | ---------------------- |
| Язык          | Rust                   |
| Web           | Axum, Tower            |
| DB            | PostgreSQL             |
| ORM           | SQLx                   |
| Cache         | Redis                  |
| Serialization | Serde                  |
| Config        | dotenv / custom config |
| Logging       | tracing                |
| Migrations    | sqlx-cli               |

---

## 🗂️ Структура проекта

```text
apps/api
├── migrations/
├── database/
├── repositories/
├── src/
│   ├── main.rs
│   ├── app.rs
│   ├── util.rs
│   ├── app/
│   └── routes/
└── Cargo.toml
```

---

## 🧪 Тестирование

* unit-тесты доменной логики
* интеграционные тесты репозиториев
* тесты API (HTTP)

---

## 🔐 Конфигурация и режимы

Поддерживаемые режимы:

* `DEV` — расширенное логирование, debug-уровень
* `PROD` — минимальный уровень логов, повышенная стабильность

Режим задаётся через переменные окружения.

---

## 🚧 Статус

API находится в активной разработке. Интерфейсы могут изменяться до стабилизации версии `v1`.

---

## 📌 Роль в экосистеме QTS

API — это:

* единый источник истины данных безопасности
* центр доменной логики
* точка интеграции для UI и аналитики
